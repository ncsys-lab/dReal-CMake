# Module: Invset (`group__invset`) — stub

## What it is

`capd::invset`: invariant-set computation. The extracted module page lists a single class,
`capd::invset::CubicalMap<MapT, GraphT, ResolutionT>` — a cubical (grid-based) representation
of a map for combinatorial/graph analysis of invariant sets. The module page has no narrative
beyond the class list.

## dReal status

**Unused** and not on `dreal-capd-usage.md`'s audit list.

## Why it might matter

Unlikely to. Cubical/graph invariant-set methods serve global dynamics analysis (Conley-index
style), not the bounded-time rigorous flow enclosure dReal needs. No fit to terminal-time
gating or parameter uncertainty. Recorded for completeness.

## Source

[`../../../CAPD/docs/html/group__invset.html`](../../../CAPD/docs/html/group__invset.html)
