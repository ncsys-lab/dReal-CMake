# Chapter: Packages

Source: [`packages.rst.txt`](../../../ibex-docs/_sources/packages.rst.txt) ·
`packages.html`. IBEX's plugin/package mechanism (how optional modules like the
affine-arithmetic or ENSTA-robotics plugins are added at configure time).

> **dReal status:** not used — dReal builds the core fork with a fixed flag set
> ([install-cmake](install-cmake.md)) and **no plugins**. Relevant only to the
> `LinearizerAffine2` idea, which lives in the `ibex-affine` *package* not built by
> dReal (see [`../AUDIT.md`](../AUDIT.md) D).
